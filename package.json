{
  "name": "chat-web",
  "version": "0.1.0",
  "private": true,
  "description": "低耦合静态网页：Ollama 聊天 + SIoT 数据可视化（无后端）",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.ts",
    "watch": "node scripts/build.ts --watch --serve",
    "serve": "node scripts/serve.ts",
    "dev": "node scripts/build.ts --watch --serve"
  },
  "dependencies": {
    "echarts": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
